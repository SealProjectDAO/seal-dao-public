# Use vendored dependencies for offline/reproducible builds.
# To switch back to crates.io, comment out these lines.
#
# To update vendored sources:
#   ./scripts/vendor-update.sh

[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor/"
